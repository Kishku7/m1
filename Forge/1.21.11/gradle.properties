org.gradle.jvmargs=-Xmx3G
org.gradle.daemon=false

mod_version=0.17.5
minecraft_version=1.21.11
# PINNED BACK to 61.1.0 deliberately. Forge 61.1.8+ (incl. 61.2.0 recommended and 61.2.1 latest)
# ship an mcp/client/Start binpatch that collides with FG6's own inject:
# java.util.zip.ZipException: duplicate entry: mcp/client/Start.class. Not a cache problem.
# Cheap retry test: LZMA-decompress that build's binpatches.lzma and search mcp/client/Start;
# zero occurrences = it will build, non-zero = it will throw. No toolchain run needed.
# check_pins.py reporting this row BEHIND is correct and is not work.
forge_version=1.21.11-61.1.0
