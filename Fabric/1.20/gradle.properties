org.gradle.jvmargs=-Xmx2G
# 1.20 Fabric build of M1 needs JDK 17 (MC 1.20 toolchain). Pinned so the build
# does not pick the JDK24/25 that 26.x uses.
org.gradle.java.home=C:/Program Files/Eclipse Adoptium/jdk-17.0.17.10-hotspot

minecraft_version=1.20
loader_version=0.16.10

mod_version=0.13.0
maven_group=com.kishku7
archives_base_name=m1

# fabric-api 0.83.0+1.20 = the MC-1.20 build (per backport toolchain note).
fabric_api_version=0.83.0+1.20
mc_dep=>=1.20- <1.20.5
